# QuizGame - Быстрая справка для новых разработчиков

## ⚡ Главное что нужно знать:

### 1. Что это за проект?
- **Java приложение** для викторины
- Вопросы хранятся в **PostgreSQL базе данных**
- Приложение выбирает **случайный вопрос** и выводит его с вариантами ответов
- Запускается из **командной строки / консоли**

### 2. Как быстро запустить?

```powershell
# Перейти в папку проекта
cd d:\QuizizGame\quzizGame

# Запустить (БД уже работает)
cmd /c run.bat

# Или через Maven собрать + запустить
mvn clean package -q
cmd /c run.bat
```

### 3. Структура кода

**4 основных Java класса:**

| Класс | Что делает | Где? |
|-------|-----------|------|
| `App.java` | Главная программа, орхестрирует все | src/main/java/ee/anton/quizgame/ |
| `DbQuestionRepository.java` | Работа с БД - получение/добавление вопросов | Там же |
| `ConsoleQuiz.java` | Выбор случайного вопроса | Там же |
| `Question.java` | Модель данных - структура вопроса | Там же |

### 4. Как данные текут в приложении?

```
App.main()
  └─> DbQuestionRepository.findAll()
      └─> Подключение к PostgreSQL
      └─> SELECT * FROM questions
      └─> Возвращает List<Question>
  
  └─> ConsoleQuiz.askQuestion()
      └─> Выбирает СЛУЧАЙНЫЙ Question
  
  └─> Question.displayQuestion()
      └─> Выводит: текст вопроса + варианты ответов
```

### 5. Файлы которые часто изменяются

```
insert_data.py          # Добавляй сюда новые вопросы для загрузки в БД
App.java                # Измени логику приложения (добавляй ответы пользователя, очки и т.д.)
Question.java           # Добавляй методы для обработки вопросов
ConsoleQuiz.java        # Расширяй логику викторины (фильтрация, сложность и т.д.)
```

---

## 📚 Основные концепции

### Maven (pom.xml)
- Система управления проектом и зависимостями
- `mvn clean package` - собирает весь проект
- Результат в папке `target/`

### PostgreSQL (база данных)
- Работает в Docker контейнере (quiz-db)
- Таблица `questions` с полями:
  - `id` - уникальный ID
  - `question_text` - текст вопроса
  - `options` - массив ответов
  - `correct_option` - индекс правильного ответа
  - `question_type` - тип вопроса

### JDBC (Java Database Connectivity)
- Способ подключения Java к БД
- Используется `PreparedStatement` для защиты от SQL-инъекций
- Кодировка важна: `?characterEncoding=UTF-8&useUnicode=true`

### UTF-8 кодировка
- КРИТИЧНА для русского текста!
- Используется в 3 местах:
  1. Консоль: `chcp 65001` (в run.bat)
  2. Java: `-Dfile.encoding=UTF-8` (в run.bat)
  3. БД: `?characterEncoding=UTF-8` (в DbQuestionRepository.java)

---

## 🔄 Типичные задачи и как их делать

### Задача 1: Добавить новый вопрос

**Способ 1 - Через Python скрипт:**
1. Открыть `insert_data.py`
2. Добавить в список `questions`:
```python
("Новый вопрос?", ["вариант1", "вариант2", "вариант3", "вариант4"], 0, "multiple_choice"),
```
3. Запустить: `python insert_data.py`

**Способ 2 - Через Docker SQL:**
```powershell
docker exec quiz-db psql -U quiz -d quizdb -c "INSERT INTO questions VALUES (NULL, 'Новый вопрос?', '{вариант1,вариант2,вариант3,вариант4}', 0, 'multiple_choice');"
```

### Задача 2: Добавить получение ответа от пользователя

**Отредактировать App.java:**
```java
public static void main(String[] args) throws Exception {
    DbQuestionRepository repo = new DbQuestionRepository();
    List<Question> dbQuestions = repo.findAll();
    
    ConsoleQuiz consoleQuiz = new ConsoleQuiz(dbQuestions);
    Question question = consoleQuiz.askQuestion(dbQuestions);
    question.displayQuestion(question);
    
    // ДОБАВИТЬ:
    Scanner scanner = new Scanner(System.in);
    System.out.print("Ваш ответ (0-3): ");
    int userAnswer = scanner.nextInt();
    
    if (userAnswer == question.getCorrectOption()) {
        System.out.println("✓ Правильно!");
    } else {
        System.out.println("✗ Неправильно!");
    }
}
```

### Задача 3: Лучше отформатировать вывод вопроса

**Отредактировать Question.java - метод displayQuestion():**
```java
public void displayQuestion(Question question) {
    List<String> variants = new ArrayList<>(List.of("A","B","C","D","E","F"));
    System.out.println("\n═══════════════════════════════════════");
    System.out.println("❓ " + question.getQuestionText());
    System.out.println("───────────────────────────────────────");
    for (int i = 0; i < question.getOptions().size(); i++) {
        System.out.println(variants.get(i) + ") " + question.getOptions().get(i));
    }
    System.out.println("═══════════════════════════════════════\n");
}
```

### Задача 4: Пересобрать проект после изменений

```powershell
cd d:\QuizizGame\quzizGame
mvn clean package -q
cmd /c run.bat
```

---

## 🐛 Частые ошибки и как их исправить

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `java.lang.ClassNotFoundException` | Класс не найден | Пересобрать: `mvn clean package` |
| `ERROR: relation "questions" does not exist` | Таблица БД не создана | Запустить: `docker exec quiz-db psql -U quiz -d quizdb -c "CREATE TABLE questions (...)"` |
| `?????` вместо русского текста | Неправильная кодировка | Запустить через: `cmd /c run.bat` (не прямо из PowerShell!) |
| `Connection refused` | БД не запущена | Запустить: `docker-compose up -d` |
| `error: not on the classpath` | Неправильная структура Maven | Убедиться что файлы в: `src/main/java/ee/anton/quizgame/` |

---

## 🎮 Как вещи работают изнутри

### Подключение к БД (DbQuestionRepository)
```java
// Создается соединение через JDBC драйвер PostgreSQL
Connection conn = DriverManager.getConnection(URL, USER, PASSWORD);

// Подготавливается SQL запрос
PreparedStatement ps = conn.prepareStatement("SELECT * FROM questions");

// Выполняется запрос
ResultSet rs = ps.executeQuery();

// Результаты преобразуются в объекты Java
while (rs.next()) {
    // Создание Question объекта из строки таблицы
}

// Соединение автоматически закрывается (try-with-resources)
```

### Выбор случайного вопроса (ConsoleQuiz)
```java
// ThreadLocalRandom.current().nextInt(size)
// Выбирает число от 0 до size-1
// Например, если в БД 3 вопроса, выберет 0, 1 или 2
```

### Вывод вопроса (Question)
```java
// Просто выводит текст в консоль
System.out.println(questionText);
for (String option : options) {
    System.out.println(option);
}
```

---

## 📖 Импортные файлы для изучения

1. **pom.xml** - как собирается проект
2. **DbQuestionRepository.java** - как работает БД
3. **App.java** - главной логика приложения
4. **run.bat** - как правильно запустить (с UTF-8)

---

## 🔐 Важные правила

1. **НИКОГДА** не конкатенируй SQL строки напрямую!
   ```java
   ❌ НЕПРАВИЛЬНО: "SELECT * FROM questions WHERE id = " + id
   ✅ ПРАВИЛЬНО: PreparedStatement с плейсхолдерами (?)
   ```

2. **ВСЕГДА** используй UTF-8 для кодировки
   ```java
   ✅ -Dfile.encoding=UTF-8
   ✅ ?characterEncoding=UTF-8
   ```

3. **ВСЕГДА** закрывай соединения (используй try-with-resources)
   ```java
   ✅ try (Connection conn = getConnection()) { ... }
   ❌ Не забывай conn.close()
   ```

4. **ВСЕГДА** используй переменные окружения для конфигурации
   ```java
   ✅ System.getenv().getOrDefault("DB_HOST", "localhost")
   ❌ Не пиши хардкод: "localhost"
   ```

---

## 🚀 Что дальше?

**Легкие задачи (начни отсюда):**
1. Добавить проверку правильности ответа
2. Добавить очки/счет
3. Красиво отформатировать вывод

**Средние задачи:**
1. Цикл для нескольких вопросов
2. Фильтрация вопросов по типу/сложности
3. Сохранение результатов в БД

**Сложные задачи:**
1. Web-интерфейс (Spring Boot)
2. Система пользователей и рейтинга
3. Мобильное приложение

---

## 📞 Git информация

```
Repository: RememberJava
Owner: KotBegemotEST
Branch: main
```

Всегда коммитай изменения в main ветку!

---

**Обновлено:** 11 ноября 2025

Если что-то не ясно - спрашивай у ИИ с этой документацией!
